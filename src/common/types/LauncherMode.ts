/**
 * @file LauncherMode.ts
 * @description The enum for the launcher mode (client or server).
 * @author Mica Technologies
 */

/**
 * The enum for the launcher mode (client or server).
 */
export enum LauncherMode {
  CLIENT = 'CLIENT',
  SERVER = 'SERVER',
}
